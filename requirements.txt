# requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# Core
Django>=4.2,<5.0
djangorestframework>=3.15,<4.0

# CORS headers (allows React dev server to call Django API)
django-cors-headers>=4.3,<5.0

# Static files serving on Render.com (replaces Nginx for static assets)
whitenoise>=6.6,<7.0

# Production WSGI server
# Using gthread workers instead of gevent — no C compilation required,
# supports Python 3.13, and handles SSE connections cleanly via OS threads.
# Each worker spawns --threads N OS threads; one thread per open SSE connection.
gunicorn>=21.2,<22.0

# Postgres driver (used on Render; ignored if DATABASE_URL is not set)
psycopg2-binary>=2.9,<3.0

# Parse DATABASE_URL environment variable provided by Render
dj-database-url>=2.1,<3.0