# .env.local.example
# Vite's dev proxy (vite.config.ts) forwards:
#   /api/** → Django :8000
#   /sse/** → Django :8000
# This is the zero-friction setup — no CORS to configure, no origin mismatch.

VITE_API_BASE_URL=